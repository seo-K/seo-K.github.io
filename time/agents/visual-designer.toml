model = "gpt-5.3-codex"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = "Own visual direction, typography, spacing, hierarchy, and interaction tone for the portfolio. Propose concrete UI patterns without drifting into implementation details."
